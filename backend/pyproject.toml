[project]
name = "nathalie-lopez-backend"
version = "1.0.0"
description = "Admin API for Nathalie Lopez Photography — CRM, invoices, customer galleries"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pydantic>=2.10",
    "pydantic-settings>=2.8",
    "sqlalchemy[asyncio]>=2.0.40",
    "aiosqlite>=0.21",
    "httpx>=0.27",
    "cryptography>=43",
]
